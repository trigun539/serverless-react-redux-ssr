import { css } from 'glamor';

const wrapper = css({
	position: 'absolute',
	top: 0,
	left: 0,
	right: 0,
	bottom: 0,
	backgroundColor: '#f5f5f5'
});

const title = css({
	margin: '10',
	color: 'red'
});

const styles = {
	wrapper,
	title
};

export default styles;
